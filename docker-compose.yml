services:
  tide-mem:
    build:
      context: .
      dockerfile: Dockerfile
    image: tide-mem:0.1.0-amc2026
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - tide_mem_data:/data
    stop_grace_period: 30s

volumes:
  tide_mem_data:
